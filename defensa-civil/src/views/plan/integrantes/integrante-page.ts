export const integrantePage = async(id: string) => {
    const main = document.createElement("main");
    const titulo = document.createElement("h1");

    titulo.textContent = "xD"
    main.append(titulo)
    return main
}